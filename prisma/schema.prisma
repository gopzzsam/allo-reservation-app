generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Reservation {
  id            Int      @id @default(autoincrement())
  customerName  String
  productName   String
  quantity      Int
  createdAt     DateTime @default(now())
}